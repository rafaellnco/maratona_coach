[project]
name = "maratona-coach"
version = "0.1.0"
description = "Maratona Coach — Telegram polling (JustRunMy deploy)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
